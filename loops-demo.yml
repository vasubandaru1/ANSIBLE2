- name: demo on loops
  hosts: localhost
  vars:
    fruits: [ "mango", "apple" ]
  tasks:
    - name: print fruits
      ansible.builtin.debug:
        msg: "fruits = {{items}}"
      loops: {{fruits}}
